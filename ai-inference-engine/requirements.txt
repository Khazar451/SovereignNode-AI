# SovereignNode AI — Inference Engine Requirements (Ollama Edition)
# Install: pip install -r requirements.txt
#
# This version uses Ollama for LLM inference (Qwen 2.5-3B).
# Ollama must be installed separately: https://ollama.com/download
# Pull the model: ollama pull qwen2.5:3b

# ── Web server ────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.0
httpx==0.27.0
requests>=2.32.3

# ── Embedding model (CPU-optimised, no GPU required) ─────────────────────────
sentence-transformers>=3.0.1
torch>=2.3.0
sentencepiece>=0.2.0
protobuf>=4.25.0
huggingface-hub>=0.23.2

# ── Vector store & document processing ───────────────────────────────────────
chromadb>=0.5.0
pypdf>=4.2.0
chardet>=5.2.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66.0
rich>=13.7.0
python-dotenv>=1.0.0

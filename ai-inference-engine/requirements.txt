# SovereignNode AI — Offline RAG Inference Engine
# Install: pip install -r requirements.txt
#
# CUDA setup (run first):
#   pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cu121
# Flash Attention 2 (optional, Ampere+ GPU only):
#   pip install flash-attn --no-build-isolation

# ── Web server ────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# ── ML / AI stack ─────────────────────────────────────────────────────────────
# Pinned to last known-good set: transformers 5.x has a NameError bug in
# integrations/accelerate.py (nn used before import). 4.44.2 is stable.
transformers==4.44.2
accelerate==0.33.0
bitsandbytes==0.43.3
torch==2.3.0
sentencepiece>=0.2.0
protobuf>=4.25.0
sentence-transformers==3.0.1
huggingface-hub>=0.23.0

# ── Vector store & document processing ───────────────────────────────────────
chromadb>=0.5.0
pypdf>=4.2.0
chardet>=5.2.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66.0
rich>=13.7.0
python-dotenv>=1.0.0
